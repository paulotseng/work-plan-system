1.刚刚批量导入后，我发现一个需求，就是旧的多个任务我要删除，太低效了，得要一个一个点选删除、确认。我希望有批量删除的功能。实现方式可以为：页面右上角有个"批量删除"的按钮，按下之后，每笔数据最左边出现勾选匡(check box)，我勾选完，跳出确认视窗，然后实现删除...。这只是我的参考方案，具体实现方式你可以帮我设计更优秀的方法。
2.我的工作计划页面：上方增加一个数据筛选标签"职能细分"。如果可以的话，最好能实现和其他筛选标签的连动，例如我已经选了"职能<管理三角>"的"业务发展维度"标签，那么，在"职能细分"标签中，就只能选择业务发展维度之下的职能细分选项。
3.批量导入数据后，我发现有些字段的宽度出现影响：a."任务描述"通常字数比较多，在呈现时被缩减了，并且以"..."的方式提示描述文字未完成。请加宽该字段的固定宽度，至少要能显示25个中文字，显示不下时才以"..."显示。b."覆盖范围"由于有些数据选择了四个以上的标签，该字段变宽了，能否固定为三个标签左右的宽度，并且允许该字段换行。 c."备注"字段文字通常比较多，这里被无限延长宽度了，请固定在25个中文字左右，允许换行，显示不下时以"..."显示。
4.批量导入的数据，都没有显示开始时间和结束时间，是不是格式问题？我在excel中呈现的是"3月2日"这样的中文日期，也许是格式造成？请解析问题和调整方案。
5.我的工作计划页面：一级标题和二级标题，我希望能像excel的冻结视窗效果那样固定住，否则往下滑动查看时，有时候会忘了自己在看什么数据。
6.我的工作计划页面：我现在以90%的浏览器缩放比例，一次也才能看到六笔数据而已，太少了。我想让在单页呈现的数据再多一些。例如，最下面的留白空间有点多，有点浪费，能否把数据页的长度拉长一点。例如，数据表的每一行数据的单元格上下留白空间减少一点，当然也不能太挤，否则眼睛难受，你来斟酌缩减。